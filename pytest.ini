[pytest]
# Put the repo root on sys.path so `import ambilight.*` resolves, and use
# importlib import mode so a stray top-level __init__.py doesn't make pytest
# treat the repo root as a package during collection.
pythonpath = .
testpaths = tests
addopts = --import-mode=importlib
